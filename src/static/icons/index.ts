import {
  House,
  Grid,
  User,
  Document,
  ArrowDown,
  Lock,
  DArrowLeft,
  Expand,
  Fold,
} from "@element-plus/icons-vue";

export const routerIcon = {
  House,
  Grid,
  User,
  Document,
  ArrowDown,
  Lock,
  DArrowLeft,
  Expand,
  Fold,
};

export const AllIcons = {
  ...routerIcon,
};
